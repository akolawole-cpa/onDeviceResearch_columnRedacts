# Data Paths Configuration

# Base paths
base_path: "/mnt/delta/"
bronze_path: "/mnt/delta/bronze/"
silver_path: "/mnt/delta/silver/"
gold_path: "/mnt/delta/gold/"
checkpoint_path: "/mnt/delta/checkpoints/"

# Project repository path for wonky studies
project_repository_path: "/mnt/project-repository-prod"

# Table names
tables:
  user: "user"
  task_complete: "task_complete"
  respondent_info: "respondent_info"
  task: "task"

# Output file paths (saved in misc folder)
output_files:
  user_info_df: "dbfs:/FileStore/misc/user_info_df_pullcomplete.parquet"
  wonky_user_counts: "dbfs:/FileStore/misc/wonky_user_counts_pullcomplete.parquet"
  wonky_respondent_df: "dbfs:/FileStore/misc/wonky_respondent_df_pullcomplete.parquet"
  wonky_respondent_summary: "dbfs:/FileStore/misc/wonky_summary_pullcomplete.parquet"
  respondent_features: "dbfs:/FileStore/misc/respondent_features_eda.parquet"

# Data filters
filters:
  country: "GB"
  min_date: "2025-09-11"
  task_origin: "odr"